# 路线图

## 当前状态：M1 开发中

**有了能运行的程序，但没有可下载的二进制，也还不适合实际使用。** 在 Linux 上从源码编译，
两台机器之间能经加密隧道 ping 通；Windows 上，守护进程和虚拟网卡在 CI 的虚拟机里实测过。

这一页不打算粉饰进度。下面每一项后面的状态标记是真实的：

| 标记 | 含义 |
| --- | --- |
| 设计中 | 在讨论方案，还没动代码 |
| 开发中 | 有代码在写，但还不能用 |
| 可用 | 能跑，有人在用 |

截至今天，**M1 的主干在 Linux 上跑通了，但没有任何一项达到「可用」** —— 还没有人在真实网络里用它，
M1 的目标（两台 **Windows** 机器）也还没达到。

## M0 地基

| 项目 | 状态 |
| --- | --- |
| 仓库、官网、设计文档 | 可用 |
| Rust workspace 与 crate 划分 | 可用 |
| 控制面 / 数据面接口契约 | 开发中 |
| 威胁模型 | 可用 |

- **crate 划分**已经定了，但还有几个 crate 没建，等各自有代码可写时再建，
  见[接口契约 · crate 划分](/guide/interfaces#crate-划分)。
- **[接口契约](/guide/interfaces)**已经有了第一个实现（M1 的数据面）来检验它。
  另一侧的控制面还没写，契约还会改，所以仍是「开发中」。
- **[威胁模型](/guide/threat-model)**是初版，M1 实现时会逐条对照。

## M1 打通主干

目标：**两台 Windows 机器，一条加密隧道，能互相 ping 通。**

| 项目 | 状态 |
| --- | --- |
| 节点身份与注册（公钥即身份，Noise IK 认证） | 开发中 |
| boringtun 集成 | 开发中 |
| wintun 虚拟网卡 | 开发中 |
| 控制面最小实现（发现 + 密钥分发） | 开发中 |
| 端点探测（类 STUN） | 开发中 |
| UDP 打洞协调 | 开发中 |
| Relay 回退 | 开发中 |

在 Linux 上用网络命名空间模拟机器和路由器，各跑一个 `meshorad`，用真的 `ping` 验证，
并检查两边最终选定的路径。CI 每次提交都跑：

| 场景 | 最终路径 |
| --- | --- |
| 两台机器在同一网段 | 直连 |
| 同一网段，但只允许走中继 | 中继 |
| 两边各在一个普通 NAT 后面（家用路由器的典型配置） | 打洞成功，直连 |
| 接着掐断两个 NAT 之间的 UDP | 几秒后回落中继（测试里约 8 秒） |
| 再恢复 | 几秒后切回直连（测试里 1 到 4 秒） |
| 一边的路由器换了公网地址（运营商重新分配） | 重连、重新探测、重新打洞，约 15 秒后回到直连 |
| 一边在对称 NAT 后面（每个目标换一个外部端口） | 打洞不通，中继 |

还差的：

- **Windows**：CI 的 Windows 虚拟机上，真的 `meshorad.exe` 建起 wintun 网卡，和同一台机器上的另一个节点
  经加密隧道收发报文，直连和经中继都通。但还没在两台真的 Windows 机器之间试过 —— 这是 M1 目标里明写的
- **打洞**：测试里的 NAT 是用 Linux 的 iptables 模拟的。真实网络里的家用路由器、运营商的 NAT
  行为各异，还没验证过
- **回落要几秒**：直连断了，要 6.5 秒没有回应才算断、切到中继，这段时间里的报文会丢
- **中继**：节点只用协调服务给的第一个中继；控制面只探测直连，不探测中继 —— 中继丢包、变慢
  甚至断了，它都察觉不到，也不会换一个

这是整个项目最关键的里程碑。**M1 跑通之前，其余全是纸面工作。**

## M2 成网

| 项目 | 状态 |
| --- | --- |
| Virtual LAN（overlay 地址分配） | 设计中 |
| 广播 / 组播转发 | 设计中 |
| 链路质量探测与自动选路 | 设计中 |
| 网络变化后的重连（换 Wi-Fi 不断线） | 设计中 |
| ACL | 设计中 |

## M3 能力编排

| 项目 | 状态 |
| --- | --- |
| Gateway（出口节点） | 设计中 |
| Game Node（低延迟档位） | 设计中 |
| 能力声明与授权模型 | 设计中 |

## M4 分应用路由

| 项目 | 状态 |
| --- | --- |
| 按 IP / 端口分流 | 设计中 |
| 按域名分流（DNS 拦截） | 设计中 |
| 按进程分流（Windows WFP） | 设计中 |

排在最后是因为它工程量最大，而且[代价明确](/guide/capabilities#application-routing)。

## 平台

| 平台 | 状态 |
| --- | --- |
| Windows（wintun） | 开发中 |
| Linux（含服务器 / NAS） | 开发中 |
| macOS | 设计中 |
| Android / iOS | 设计中 |
| OpenWrt | 设计中 |

Windows 优先，因为开发机是它，而且最难的分应用分流在 Windows 上 —— 先啃硬的能更早暴露设计问题。

不过眼下实测跑得最多的反而是 Linux：开发用的云端环境是 Linux，虚拟网卡和端到端测试在那里能真跑。
Windows 这边靠 CI 的 Windows 虚拟机：除了编译、lint、单元测试，还真的建 wintun 网卡、跑守护进程。

## 没有时间表

这是个人项目，给不出交付日期，写一个出来也是骗人。

进展会体现在 [commit 历史](https://github.com/Kerxs/meshora/commits/main)和这一页的状态标记上。
想参与讨论见[参与进来](/guide/contributing)。
