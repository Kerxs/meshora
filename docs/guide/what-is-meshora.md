# 它解决什么问题

::: warning 当前状态
Meshora 还在**开发中**（M1）。本页描述的是目标形态，不是已经实现的功能。见[路线图](/guide/roadmap)。
:::

## 两台设备想直接说上话，为什么这么难

把两台在不同网络里的设备连起来，听上去应该是件简单的事。实际上要依次跨过这些坎：

- **它们不知道对方在哪。** 家宽的公网 IP 会变，手机在基站之间漂移，没有一个稳定的地址可言。
- **它们都躲在 NAT 后面。** 双方都只能主动往外连，谁都无法被动接受连接。运营商级 NAT（CGNAT）让情况更糟 —— 你连自己的公网 IP 都没有。
- **就算连上了，也不知道对方是谁。** IP 不是身份。
- **有时候根本连不上。** 对称型 NAT 撞上对称型 NAT，打洞成功率会显著下降，必须有中继兜底。
- **网络还会变。** 从 Wi-Fi 切到蜂窝网，刚建好的连接就断了，得重来一遍。

现有的解决办法基本分两类，各有各的代价：

| 办法 | 代价 |
| --- | --- |
| 端口转发 / DDNS | 要有公网 IP，要会配路由器，CGNAT 下直接失效 |
| 内网穿透（frp 等） | 所有流量绕服务器，延迟和带宽成本都由你承担 |
| 传统 VPN | 星型拓扑，两台设备通信也要先绕一圈中心节点 |

## Meshora 的取法

把上面那串步骤全部自动化，收进一条固定的流水线：

<Pipeline />

设备接入时只需要证明"我是谁"（公钥即身份），剩下的交给系统。
能直连就直连，直连不了就走中继，网络变了就重新选路 —— 这些都不需要你知道。

## 不止是"连上"

如果只做到设备互通，那就是又一个 VPN。Meshora 想多做一步：**让节点对外提供不同的网络能力**。

一台有公网的机器可以同时是中继和出口；一台 NAS 可以只参与虚拟局域网；
一台游戏本可以在需要时切到低延迟档位。能力是按节点声明的，网络据此编排路径。

这就是"[Connect Everything](/guide/capabilities)"想表达的意思：连接的不只是设备，还有它们各自能提供的网络能力。

## 它不适合什么

把话说在前面，免得你花时间读完才发现不对路：

- **不是匿名网络。** Meshora 的目标是连通性和可控性，不是隐藏你是谁。它不提供 Tor 那样的匿名性。
- **不是翻墙工具。** Gateway 能力的设计目的是给自己的设备提供一个可选出口，比如出差时回家上网。
- **不打算做无服务器的纯 P2P。** 节点发现和穿透协调需要一个协调点。控制面可以自建，但它存在。

下一步：[核心概念](/guide/concepts) 会把 Node、能力、路径这三个词说清楚。
