# 安全策略

## 当前状态

**Meshora 尚无任何发布版本。** 项目处于设计阶段，没有可运行的网络引擎，也没有可下载的二进制。

因此目前不存在"受影响的版本"。但渠道先立在这里，因为有两类问题现在就值得报告。

## 现在就值得报告的问题

### 1. 设计缺陷

这是现阶段**最有价值**的安全反馈。如果你在[设计文档](https://kerxs.github.io/meshora/guide/architecture)里
看出协议层面的问题，请告诉我：

- 认证与密钥分发的缺陷
- 重放攻击、降级攻击
- 控制面被攻陷后的影响范围（爆炸半径）
- 中继的元数据暴露超出了文档已承认的范围
- ACL 模型的越权路径

设计阶段改方案的成本，比实现之后再改低两个数量级。**现在挑出来最划算。**

[威胁模型](https://kerxs.github.io/meshora/guide/threat-model)列出了已经承认的风险和信任假设。
找出它没写到的，就是最有价值的报告。

### 2. 站点与构建链的问题

官网是现在唯一在运行的东西。Rust 代码还不收发任何报文，但它的依赖和 CI 一样可能被投毒。
XSS、依赖投毒（npm 和 crates.io 都算）、构建链问题、CI 配置泄漏 —— 这些都算。

## 怎么报告

**不要开公开 Issue。** 请走这两个渠道之一：

- GitHub 的 [Private vulnerability reporting](https://github.com/Kerxs/meshora/security/advisories/new)（推荐）
- 邮件：通过 GitHub 账号 [@Kerxs](https://github.com/Kerxs) 联系

报告里请尽量包含：问题描述、影响范围、复现步骤或推理过程。

## 承诺

这是个人项目，不是有安全团队的公司，所以只承诺做得到的事：

- **7 天内**回复确认收到
- 会告诉你我的判断：是问题、不是问题、还是暂时无法判断
- 修复后如果你愿意，会在致谢里署名
- **不会**因为问题不好听就装作没看见

给不出行业标准的响应时限，写一个出来也是骗人。

## 范围之外

- 对第三方依赖已公开且已有上游修复的漏洞，直接开 Issue 即可，不必走私密渠道
- 纯理论、无实际影响的问题，欢迎讨论，但按普通 Issue 处理
